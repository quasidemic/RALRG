# RAG Record Search

Open `index.html` in a browser and select one or more JSONL files produced by `src/rag/rag_records.py`, such as `records_theory.jsonl`.

The app reads the selected files locally, builds the field selector from the keys found in the records, and searches across record values. No server or build step is required.
